{
  "name": "pinkvanity",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "dev": "node scripts/dev-extension.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "node scripts/clean.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}

